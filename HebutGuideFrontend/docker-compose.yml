services:
  hebutguide-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hebutguide-frontend
    ports:
      - "5174:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

